# Gemini Bridge 설치

1. 일반 Chrome에서 `chrome://extensions`를 엽니다.
2. 개발자 모드를 켜고 `압축해제된 확장 프로그램을 로드합니다`를 선택합니다.
3. 이 `browser_extension` 폴더를 선택합니다.
4. 이미 로그인된 `https://gemini.google.com/` 탭을 새로고침합니다.

피드 실행 때 DevTools나 별도 Chrome 창은 열리지 않습니다. 브리지는 로컬 주소 `127.0.0.1:43127`에만 바인딩하며 별도 토큰 입력이 필요하지 않습니다.
